# Solar Challenge Week 0

This project is part of the 10 Academy KAIM program — Week 0 Challenge.

## Setup Instructions

1. Clone the repository:
   ```bash
   git clone https://github.com/Jaki77/solar-challenge-week0.git
   cd solar-challenge-week0
   ```

2. Create a virtual environment and install dependencies:
   ```bash
   python -m venv venv
   source venv/bin/activate  # or venv\Scripts\activate on Windows
   pip install -r requirements.txt
   ```

3. Run the GitHub Actions CI to verify setup.
